{
  "name": "digiguard-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix backend start",
    "backend:start": "npm --prefix backend start",
    "backend:dev": "npm --prefix backend run dev",
    "frontend:react:dev": "npm --prefix frontend-react run dev"
  }
}
